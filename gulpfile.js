// Generated by CoffeeScript 1.8.0
(function() {
  var app, coffee, gulp, gutil, jade, nodemon, sass, sources;

  gulp = require('gulp');

  jade = require('gulp-jade');

  gutil = require('gulp-util');

  sass = require('gulp-sass');

  coffee = require('gulp-coffee');

  nodemon = require('gulp-nodemon');

  app = null;

  sources = {
    jade: "src/**/*.jade",
    coffee: "src/**/*.coffee",
    sass: "src/**/*.scss",
    overwatch: "src/**/*.{js,html,css}"
  };

  gulp.task("jade", function(event) {
    return gulp.src([sources.jade], {
      base: "./src"
    }).pipe(jade({
      pretty: true
    })).pipe(gulp.dest("./app"));
  });

  gulp.task('coffee', function(event) {
    return gulp.src([sources.coffee], {
      base: "./src"
    }).pipe(coffee()).pipe(gulp.dest("./app"));
  });

  gulp.task("sass", function(event) {
    return gulp.src([sources.sass], {
      base: "./src"
    }).pipe(sass({
      style: "compressed"
    })).pipe(gulp.dest("./app"));
  });

  gulp.task("watch", function() {
    gulp.watch(sources.jade, ["jade"]);
    gulp.watch(sources.sass, ["sass"]);
    gulp.watch(sources.coffee, ["coffee"]);
  });

  gulp.task("develop", function() {
    return nodemon({
      script: "app/app.js",
      ext: "html js",
      ignore: ["./app/assets/**", "./app/views/**"]
    }).on("restart", function() {
      return console.log("RESTARTED!");
    });
  });

  gulp.task("default", ["jade", "coffee", "sass", "develop", "watch"]);

}).call(this);
